name: .NET

on:
  push:
    branches: ["product"]
  pull_request:
    branches: ["product"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore GitClient.slnx

      - name: Build
        run: dotnet build GitClient.slnx --no-restore --configuration Release

      - name: Test
        run: dotnet test GitClient.slnx --no-build --configuration Release --verbosity normal
